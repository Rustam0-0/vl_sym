{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <div class="container d-flex py-3">
        <div class="col-lg-6 col-sm-12 modal_left">
            <form method="post">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-3">
                        <h3>Vous êtes connecté en tant que <strong>{{ app.user.username }}</strong>,</h3> <a
                                href="{{ path('app_logout') }}">Déconnexion</a>
                    </div>
                {% endif %}

                <h1 class="h3 mb-3 font-weight-normal">Etes-vous déjà clients chez nous?</h1>
                <label for="inputEmail">Email</label>
                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control"
                       autocomplete="email" required autofocus><br>
                <label for="inputPassword">Mot de pass</label>
                <input type="password" name="password" id="inputPassword" class="form-control"
                       autocomplete="current-password" required>

                <input type="hidden" name="_csrf_token"
                       value="{{ csrf_token('authenticate') }}"
                >
                {#
                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html #}

                <div class="checkbox mb-3">
                    <label>
                        <input type="checkbox" name="_remember_me"> Remember me
                    </label>
                </div>

                <button class="btn btn-block modal_btn" type="submit">
                    Se connecter
                </button>
            </form>
        </div>
        <div class="vr">
            <div></div>
        </div>
        <div class="col-lg-6 col-sm-12" style="line-height: 48.5px">
            <h1 class="h3 mb-3 font-weight-normal">N’êtes-vous pas encore client?</h1>
            <p>En tant que client Village Green
                vous pouvez suivre vos envoies,
                lire des tests produits exclusifs,
                évaluer des produits, déposer
                des petites annonces, gérer
                vos chèques-cadeaux, devenir
                partenaire et bien plus encore.</p>
            <a type="button" class="btn btn-block modal_btn" href="{{ path('singup') }}">S'inscrire</a>
        </div>
    </div>
{% endblock %}
